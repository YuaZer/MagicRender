This folder keeps MagicRender language source files grouped separately for maintainers.

Minecraft loads translations from `assets/magicrender/lang/*.json`, so the files in
`lang/` are the runtime source of truth. Keep this folder aligned when adding or
reviewing translations.
